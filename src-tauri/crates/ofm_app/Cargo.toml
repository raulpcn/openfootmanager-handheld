[package]
name = "ofm_app"
version = "0.1.0"
edition = "2021"

[dependencies]
domain = { path = "../domain" }
ofm_core = { path = "../ofm_core" }
engine = { path = "../engine" }
log = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rand = "0.10"
chrono = "0.4"
